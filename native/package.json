{
  "name": "@tytle-enclaves/native",
  "version": "0.1.0",
  "description": "Native Rust addon for vsock + NSM ioctl",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "name": "native",
    "triples": {
      "defaults": false,
      "additional": ["x86_64-unknown-linux-musl"]
    }
  },
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0"
  },
  "license": "AGPL-3.0",
  "files": [
    "index.js",
    "index.d.ts",
    "*.node"
  ]
}
